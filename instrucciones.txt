bueno, lo que hay que generar es un circulo que se rellene de rectangulos irregulares (trapecios).
una posible estrategia es crear una clase trapezoide. Esta clase deberia contener 4 vertices.

beginShape();
vertex();
vertex();
vertex();
vertex();
endShape(CLOSE);

desarrollar un algoritmo que nos permita dibujar trapecios automatizados.
lo intente haciendo trapecios formados en un circulo y luego deformandolo con funciones, no funciono muy bien.

ahora puedo intentar idear otra manera, randomizando los vertices de manera controlada.
te puedo decir

    x1,x2,x3,x4

    x1 = 0;
    x2 = x1 * rudio mediano;
    x3 = x2 + ruido minimo;
    x4 = x1 + rudio minimo;

    lo mismo para y
    y1 = 0;
    y2 = y1 * ruido mediano;
    y3 = y2 + ruido minimo;
    y4 = y1 + rudio minimo;

    ruido = random(-5,5); 

    puede haber un array de distancias x, y posibles para controlar el tamaño y variacion de los trapecios.

la factura puede ser hecha con muchas trazos finos horizontales que al verticalizar los rectangulos acompañen esa disposicion
